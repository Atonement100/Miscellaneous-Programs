prog1_1 should be run as ./prog1_1 [-xin|-xout|-bin|-bout]* < in > out 
*The last two in/out arguments will be applied. Defaults to -bin -xout.
No known bugs.

prog1_2 should be run as ./prog1_2 [-e|-d] -k<filename> [-x|-b] < in > out
Middle argument should look like -kKeyInput.txt, for example.
No known bugs.